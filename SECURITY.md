# Security Policy

Report suspected vulnerabilities through GitHub's private security-advisory feature.

Do not post API keys, secrets, account identifiers, state files, audit trails, order responses, infrastructure addresses, or logs in public issues.

If a credential is ever committed, revoke it immediately. Deleting the file from the latest commit does not remove it from Git history.
